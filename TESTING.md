# 测试说明

## 自动化测试

项目不依赖第三方测试框架。安装 Node.js 后，在项目根目录运行：

```bash
npm test
```

运行包含清单/权限/资源/语法检查的完整检查：

```bash
npm run check
```

`tests/run-tests.js` 覆盖需求中的十类场景：人工英文 JSON3、自动英文滚动字幕、中文 XML、多语言及同语言多轨、无字幕、SPA URL 切换、旧播放器响应、长视频、中文标点及 Unicode 文件名、自动字幕重叠去重；另有 VTT/SRT 清洗测试。

## 浏览器手动验收

1. 在 `chrome://extensions/` 开启开发者模式并加载项目根目录。
2. 分别打开具备人工英文、自动英文、中文及多语言字幕的视频。
3. 点击插件，核对标题和轨道列表；同语言多个轨道应显示“人工字幕”或“自动生成”。
4. 导出各轨道，确认 TXT 为 UTF-8、无时间戳/序号/标签且标点正常。
5. 打开无字幕视频，确认显示“当前视频没有字幕或字幕已被禁用”。
6. 不刷新页面，从一个视频点击到另一个视频，再次打开 Popup，确认标题和字幕均来自新视频。
7. 刷新视频页后重复导出。
8. 用长视频检查下载耗时和完整性。
9. 用包含全角标点的中文字幕检查字符保留。
10. 用自动字幕视频抽查文件中没有逐字滚动造成的大段重复。
11. 点击导出后立刻点击页面让 Popup 关闭，确认任务仍完成并出现成功通知。
12. 导出过程中重新打开 Popup，确认显示后台任务状态且不会重复启动任务。
13. 在使用新版 `PAmodern_transcript_view` 文字稿面板的视频上导出，确认插件能读取 `transcript-segment-view-model` 段落；回归样例：`vkpS7WztTMc`。

YouTube 的视频可用性、地区限制、登录要求和字幕轨道会变化，因此仓库不写死依赖某个公开视频 ID。发布前建议用目标地区和账号状态下的公开视频重新执行以上验收。

## 预期结果

- Popup 不因字段缺失崩溃。
- 没有字幕、字幕为空、请求失败均有明确状态。
- 下载文件名不含操作系统非法字符，重复下载由 Chrome 自动追加编号。
- 所有字幕仅在扩展内存中短暂处理，不写入扩展存储或外部服务器。
